import { createPlaceholderDeck } from "./schema.js";

export const n1Cards = Object.freeze([]);

export const n1Deck = createPlaceholderDeck({
  id: "jlpt-n1",
  level: 1,
  label: "N1",
});

export const n1CardsById = Object.freeze({});
